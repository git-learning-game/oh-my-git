title = new_title_081
cards = add commit

[description]

new_description_082

[cli]

cli_new_006

[setup]

echo "@@setup_auto_038@@" > candle

[win]

# new_win_hint_083
test "$(git diff --cached --name-only)" = "candle" || file -f .git/candle-added && touch .git/candle-added

# new_win_hint_084
test "$(git ls-tree --name-only HEAD)" = "candle"
