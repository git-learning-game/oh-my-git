title = reflog_title_213
cards = checkout reflog

[description]

reflog_description_214

[setup]

for i in {1..10}; do
    git commit --allow-empty -m $i
    git branch $i
done
git checkout 3
git checkout main

[win]

# reflog_win_hint_215
test "$(git rev-parse HEAD)" = "$(git rev-parse 3)"
