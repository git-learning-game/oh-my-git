title = pushedsomethingbroken_title_216
cards = revert push

[description]

pushedsomethingbroken_description_217

[setup]

echo "@@setup_auto_211@@" > text
git add .
git commit -m fine
echo "@@setup_auto_212@@" > text
git commit -am "also fine"
echo "@@setup_auto_213@@" > text
git commit -am "very bad"
echo "@@setup_auto_214@@" > text
git commit -am "fine again"

git push team main
git branch -u team/main main

[setup team]

[win team]

# pushed_win_hint_1
! { git show main:text | grep -q "very bad"; }
# pushed_win_hint_2
git show main^:text | grep -q "very bad"
