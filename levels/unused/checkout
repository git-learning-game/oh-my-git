title = checkout_title_204
cards = checkout-file

[description]

checkout_description_205

[setup]

echo "A" >> essay.txt
git add .
git commit -m "Initial commit"

echo "B" >> essay.txt
git commit -a -m "Improved version"

echo "C" >> essay.txt
git commit -a -m "Even better version"

echo "D" >> essay.txt
git commit -a -m "Marvelous version"

echo "@@setup_auto_053@@" > essay.txt

[win]

# checkout_win_hint_206
cat essay.txt | grep D
