title = commit_title_188
cards = add reset checkout commit

[description]

commit_description_189

[setup]

echo a > a
echo x > b
echo x > c
git add .
git commit -m "Initial commit"
echo x > a
echo b > b
git add b
echo c > c

[win]

# commit_win_hint_190
test "$(git show main:a)" = x
# commit_win_hint_191
test "$(git show main:b)" = x
# commit_win_hint_192
test "$(git show main:c)" = x
