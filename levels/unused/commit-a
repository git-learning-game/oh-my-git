title = commita_title_176
cards = add reset checkout commit commit-a

[description]

commita_description_177

[setup]

echo a > a
echo b > b
echo c > c
git add .
git commit -m "Initial commit"
echo x > a
echo x > b
echo x > c

[win]

# commita_win_hint_178
test "$(git show main:a)" = x && test "$(git show main:b)" = x && test "$(git show main:c)" = x
